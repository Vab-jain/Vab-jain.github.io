# macOS
.DS_Store*

# Hugo build & cache
public/
resources/
hugo_stats.json
.hugo_build.lock

# Hugo executable (if you downloaded it directly)
hugo_extended*

# Go modules (optional — if you're not pinning dependencies)
go.sum

# If you have downloaded modules or themes (unneeded if using Hugo Modules via go.mod)
# /themes/ or /modules/ — if applicable

# Temp / backup / cache files
*.bak
*.swp
*.tmp
*.log
